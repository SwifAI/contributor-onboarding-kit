[project]
name = "contributor-onboarding-kit"
version = "0.1.0"
description = "Small utility for contributor setup notes"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
